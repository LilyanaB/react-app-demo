import React from "react";
import ReactDOM from "react-dom/client";
import Element from "./components/test-component";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <Element />
  </React.StrictMode>
);
