import React from "react";
import ReactDOM from "react-dom/client";
import Demo from "./components/state.component";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <Demo />
  </React.StrictMode>
);
